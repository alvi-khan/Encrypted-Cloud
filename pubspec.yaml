name: encrypted_cloud
description:

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  googleapis: any
  google_sign_in: any
  provider: ^6.0.3
  file_picker: ^4.6.0
  aes_crypt_null_safe: ^2.0.2
  carousel_slider: ^4.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
